import { JoinAuthorsPipe } from './join-authors.pipe';

describe('JoinAuthorsPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinAuthorsPipe();
    expect(pipe).toBeTruthy();
  });
});
